{{- if or (not (hasKey .Values.ui "enabled")) (.Values.ui.enabled) }}
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "dwpk.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    control-plane: ui
    {{- with .Values.ui.service.labels }}
    {{- with omit . "app.kubernetes.io/managed-by" "app.kubernetes.io/name" "helm.sh/chart" "app.kubernetes.io/instance" "control-plane" }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- end }}
  name: {{ include "dwpk.resourceName" (dict "suffix" "ui-service" "context" $) }}
  namespace: {{ .Release.Namespace }}
  {{- with .Values.ui.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ports:
  - name: http
    port: {{ .Values.ui.service.port }}
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/name: {{ include "dwpk.name" . }}
    control-plane: ui
  type: {{ .Values.ui.service.type }}
{{- end }}
